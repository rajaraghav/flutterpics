import 'package:flutter/material.dart';

void main() {
  var app = MaterialApp(
      home: Scaffold(
        floatingActionButton: FloatingActionButton(onPressed: ()=>{print("ssup")),
    appBar: AppBar(
      title: Text("ssup"),
    ),
  ));

  runApp(app);
}
